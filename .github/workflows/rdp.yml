name: Persistent Playit RDP Tunnel

on:
  workflow_dispatch:

jobs:
  setup-rdp-tunnel:
    runs-on: windows-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    # 🔹 Enable RDP and keep it persistent
    - name: Enable and Persist RDP
      run: |
        Write-Host "🔄 Enabling RDP..."
        $rdpKeys = @(
          'HKLM:\System\CurrentControlSet\Control\Terminal Server',
          'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp'
        )
        foreach ($key in $rdpKeys) {
          Set-ItemProperty -Path $key -Name "fDenyTSConnections" -Value 0 -ErrorAction SilentlyContinue
          Set-ItemProperty -Path $key -Name "UserAuthentication" -Value 1 -ErrorAction SilentlyContinue
          Set-ItemProperty -Path $key -Name "fSingleSessionPerUser" -Value 0 -ErrorAction SilentlyContinue
        }
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
        Set-Service -Name TermService -StartupType Automatic
        Write-Host "✅ RDP Enabled Successfully!"

    # 🔹 Install WinGet (Robust Method with Multiple Fallbacks)
    - name: Install WinGet (Alternative Method)
      run: |
        Write-Host "🔄 Checking for WinGet..."
        if (-not (Get-Command winget -ErrorAction SilentlyContinue)) {
          Write-Host "🔄 Downloading WinGet..."
          
          # Download WinGet Installer
          $wingetUrl = "https://github.com/microsoft/winget-cli/releases/latest/download/Microsoft.DesktopAppInstaller_8wekyb3d8bbwe.msixbundle"
          $wingetPath = "$env:TEMP\WinGet.msixbundle"
          Invoke-WebRequest -Uri $wingetUrl -OutFile $wingetPath -UseBasicParsing

          # Attempt Install via Add-AppxPackage
          Try {
            Add-AppxPackage -Path $wingetPath -ErrorAction Stop
            Write-Host "✅ WinGet installed via MSIXBUNDLE!"
          } Catch {
            Write-Host "⚠️ Add-AppxPackage failed. Trying alternative method..."

            # Use DISM to enable Desktop App Installer if available
            Try {
              DISM /Online /Add-Capability /CapabilityName:Microsoft.DesktopAppInstaller~~~~0.0.1.0
              Write-Host "✅ WinGet installed via DISM!"
            } Catch {
              Write-Host "❌ WinGet installation failed completely. Skipping..."
            }
          }
        }

        # Verify installation
        if (Get-Command winget -ErrorAction SilentlyContinue) {
          Write-Host "✅ WinGet is installed!"
        } else {
          Write-Host "❌ WinGet installation failed."
          exit 1
        }

    # 🔹 Enable Virtualization & Hyper-V
    - name: Enable Virtualization & Hyper-V
      run: |
        Write-Host "🔄 Checking if system supports Hyper-V..."
        $hyperVSupport = (Get-WindowsOptionalFeature -Online | Where-Object { $_.FeatureName -like "Microsoft-Hyper-V*" })
        if (-not $hyperVSupport) {
          Write-Host "❌ Hyper-V is not available on this system. Skipping..."
          exit 1
        }

        Write-Host "🔄 Enabling Virtualization..."
        Try {
          bcdedit /set hypervisorlaunchtype Auto
          Write-Host "✅ Virtualization Enabled!"
        } Catch {
          Write-Host "⚠️ Failed to enable virtualization. Continuing..."
        }

        Write-Host "🔄 Enabling Hyper-V..."
        $features = @("Microsoft-Hyper-V-All", "Microsoft-Hyper-V", "Microsoft-Hyper-V-Platform", "Microsoft-Hyper-V-Management-Tools")
        foreach ($feature in $features) {
          Try {
            Enable-WindowsOptionalFeature -Online -FeatureName $feature -NoRestart -ErrorAction Stop
            Write-Host "✅ Successfully enabled $feature"
          } Catch {
            Write-Host "⚠️ Failed to enable $feature, trying next..."
          }
        }

        # Verify if Hyper-V is enabled
        $hyperVEnabled = (Get-WindowsOptionalFeature -Online | Where-Object { $_.FeatureName -eq "Microsoft-Hyper-V-All" -and $_.State -eq "Enabled" })
        if ($hyperVEnabled) {
          Write-Host "✅ Hyper-V is successfully enabled!"
        } else {
          Write-Host "⚠️ Hyper-V installation may not be complete. Checking DISM method..."
          Try {
            DISM /Online /Enable-Feature /FeatureName:Microsoft-Hyper-V-All /All /NoRestart
            Write-Host "✅ DISM method executed for Hyper-V."
          } Catch {
            Write-Host "❌ DISM method failed. Hyper-V may not be available."
          }
        }


    # 🔹 Install Playit (Multiple Methods)
    - name: Install Playit
      run: |
        Write-Host "🔄 Downloading Playit..."
        $playitUrl = "https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-windows-x86_64-signed.exe"
        Invoke-WebRequest -Uri $playitUrl -OutFile "$env:USERPROFILE\playit.exe" -ErrorAction SilentlyContinue
        if (!(Test-Path "$env:USERPROFILE\playit.exe")) {
          Write-Host "⚠️ Download failed, trying alternate source..."
          Invoke-WebRequest -Uri "https://playit.gg/downloads/playit.exe" -OutFile "$env:USERPROFILE\playit.exe"
        }
        if (Test-Path "$env:USERPROFILE\playit.exe") { Write-Host "✅ Playit Downloaded!" } else { Write-Host "❌ Playit download failed." }

    # 🔹 Register Playit as a Service
    - name: Register Playit as a Service
      run: |
        Write-Host "🔄 Registering Playit as a service..."
        sc create PlayitService binPath= "$env:USERPROFILE\playit.exe --secret $env:PLAYIT_AUTH_KEY" start= auto
        Try { sc start PlayitService }
        Catch { Write-Host "⚠️ Playit Service failed to start. Trying manual execution..."; Start-Process -FilePath "$env:USERPROFILE\playit.exe" }
        Write-Host "✅ Playit Service Registered!"

    # 🔹 Keep Session Active
    - name: Keep Session Active
      run: |
        Write-Host "🔄 Keeping RDP Session Active..."
        reg add "HKLM\Software\Microsoft\Windows NT\CurrentVersion\Winlogon" /v KeepRDPConnections /t REG_DWORD /d 1 /f
        Write-Host "✅ RDP Session Kept Active!"

    # 🔹 Keep GitHub Actions runner alive
    - name: Keep Runner Alive
      run: |
        while ($true) {
          Write-Host "🔄 Keeping runner alive... $(Get-Date)"
          Start-Sleep -Seconds 300
        }
