name: Ultra Performance RDP Tunnel
on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Session duration (hours)'
        required: false
        default: '6'
        type: string
jobs:
  ultra-performance-rdp:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: EXTREME CPU Performance Boost
      run: |
        Write-Host "🔥🔥🔥 EXTREME CPU PERFORMANCE MODE - MAXIMUM POSSIBLE 🔥🔥🔥"
        
        # Ultimate Performance Power Scheme
        powercfg -duplicatescheme e9a42b02-d5df-448d-aa00-03f14749eb61 2>$null
        $ultimate = (powercfg -list | Select-String "Ultimate").Line.Split()[3]
        if ($ultimate) { powercfg -setactive $ultimate } else { powercfg -setactive SCHEME_MIN }
        
        # ABSOLUTE MAXIMUM CPU Settings - ZERO Throttling
        $guid = (powercfg -getactivescheme).Split()[3]
        powercfg -setacvalueindex $guid SUB_PROCESSOR PROCTHROTTLEMAX 100
        powercfg -setacvalueindex $guid SUB_PROCESSOR PROCTHROTTLEMIN 100
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PROCTHROTTLEMAX 100
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PROCTHROTTLEMIN 100
        
        # Force ALL CPU cores active 24/7 - No parking EVER
        powercfg -setacvalueindex $guid SUB_PROCESSOR CPMINCORES 100
        powercfg -setdcvalueindex $guid SUB_PROCESSOR CPMINCORES 100
        powercfg -setacvalueindex $guid SUB_PROCESSOR CPMAXCORES 100
        powercfg -setdcvalueindex $guid SUB_PROCESSOR CPMAXCORES 100
        
        # EXTREME CPU Boost Settings - Beyond Normal Limits
        powercfg -setacvalueindex $guid SUB_PROCESSOR PERFBOOSTMODE 2  # Aggressive turbo
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PERFBOOSTMODE 2
        powercfg -setacvalueindex $guid SUB_PROCESSOR PERFBOOSTPOL 100  # Maximum boost policy
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PERFBOOSTPOL 100
        
        # EXTREME CPU Frequency Control - Force maximum always
        powercfg -setacvalueindex $guid SUB_PROCESSOR PROCFREQMAX 0  # Remove frequency cap
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PROCFREQMAX 0
        powercfg -setacvalueindex $guid SUB_PROCESSOR PROCFREQMIN 100  # Minimum at 100%
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PROCFREQMIN 100
        
        # Disable CPU thermal throttling (EXTREME)
        powercfg -setacvalueindex $guid SUB_PROCESSOR PROCTHROTTLEPOL 0
        powercfg -setdcvalueindex $guid SUB_PROCESSOR PROCTHROTTLEPOL 0
        
        # EXTREME Registry Performance Tweaks
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\PriorityControl" -Name "Win32PrioritySeparation" -Value 38 -EA 0  # Max foreground boost
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management" -Name "LargeSystemCache" -Value 1 -EA 0
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management" -Name "DisablePagingExecutive" -Value 1 -EA 0  # Keep kernel in RAM
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management" -Name "IoPageLockLimit" -Value 983040 -EA 0  # Max I/O performance
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "NtfsDisableLastAccessUpdate" -Value 1 -EA 0
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "NtfsDisable8dot3NameCreation" -Value 1 -EA 0
        
        # EXTREME CPU Scheduling Priority
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\PriorityControl" -Name "IRQ8Priority" -Value 1 -EA 0  # System timer highest priority
        Set-ItemProperty "HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\kernel" -Name "DpcTimeout" -Value 0 -EA 0  # Remove DPC timeout
        
        # Disable ALL power management
        powercfg -change standby-timeout-ac 0; powercfg -change hibernate-timeout-ac 0
        powercfg -change disk-timeout-ac 0; powercfg -change monitor-timeout-ac 0
        powercfg -hibernate off
        
        # Force maximum CPU state always
        powercfg -setactive $guid
        
        # EXTREME: Set system timer resolution to 0.5ms (highest possible)
        try {
            Add-Type -TypeDefinition @"
                using System;
                using System.Runtime.InteropServices;
                public class NtTimer {
                    [DllImport("ntdll.dll", SetLastError=true)]
                    public static extern int NtSetTimerResolution(uint DesiredResolution, bool SetResolution, out uint CurrentResolution);
                }
        "@
            $currentRes = 0
            [NtTimer]::NtSetTimerResolution(5000, $true, [ref]$currentRes)  # 0.5ms resolution
            Write-Host "✅ Timer resolution set to MAXIMUM (0.5ms)"
        } catch {
            Write-Host "⚠ Advanced timer optimization not available"
        }
        
        Write-Host "🚀 CPU: EXTREME PERFORMANCE MODE - ALL LIMITERS REMOVED!"
        Write-Host "🔥 CPU Frequency: UNCAPPED | Cores: 100% ACTIVE | Throttling: DISABLED"
    
    - name: System Optimization Blitz
      run: |
        # Ultra Performance Visual Settings
        Set-ItemProperty "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" -Name "VisualFXSetting" -Value 2
        Set-ItemProperty "HKCU:\Control Panel\Desktop" -Name "UserPreferencesMask" -Value ([byte[]](0x90,0x12,0x03,0x80,0x10,0x00,0x00,0x00))
        Set-ItemProperty "HKCU:\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Name "EnableTransparency" -Value 0
        
        # Aggressive Service Cleanup
        $stopServices = @("DiagTrack","dmwappushservice","WSearch","Themes","WerSvc","Fax","MapsBroker","lfsvc","WMPNetworkSvc","XblAuthManager","XblGameSave","XboxGipSvc","XboxNetApiSvc")
        foreach($s in $stopServices) { 
          try { Stop-Service $s -Force -EA 0; Set-Service $s -StartupType Disabled -EA 0 } catch {} 
        }
        
        # Memory & Storage Optimization
        $totalRAM = [math]::Round((Get-CimInstance Win32_PhysicalMemory | Measure-Object capacity -Sum).sum / 1GB)
        $cs = Get-CimInstance Win32_ComputerSystem
        if ($cs.AutomaticManagedPagefile) { Set-CimInstance -InputObject $cs -Property @{AutomaticManagedPagefile=$false} }
        Get-CimInstance Win32_PageFileSetting | Remove-CimInstance -EA 0
        New-CimInstance Win32_PageFileSetting -Property @{Name="C:\pagefile.sys";InitialSize=($totalRAM*2048);MaximumSize=($totalRAM*2048)} -EA 0
        
        # Disable Windows Defender for Max Performance
        Set-MpPreference -DisableRealtimeMonitoring $true -EA 0
        Set-MpPreference -DisableBehaviorMonitoring $true -EA 0
        Set-MpPreference -DisableIOAVProtection $true -EA 0
        
        Write-Host "✅ SYSTEM OPTIMIZED FOR MAXIMUM PERFORMANCE"
    
    - name: Ultra RDP Configuration
      run: |
        # Enable RDP with Maximum Performance
        Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
        
        $rdp = 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp'
        # Ultra Performance RDP Settings
        Set-ItemProperty $rdp -Name "MaxInstanceCount" -Value 50 -EA 0
        Set-ItemProperty $rdp -Name "fDisableWallpaper" -Value 1 -EA 0
        Set-ItemProperty $rdp -Name "fDisableFullWindowDrag" -Value 1 -EA 0
        Set-ItemProperty $rdp -Name "fDisableMenuAnims" -Value 1 -EA 0
        Set-ItemProperty $rdp -Name "fDisableTheming" -Value 1 -EA 0
        Set-ItemProperty $rdp -Name "BitmapCacheSize" -Value 65536 -EA 0  # Max cache
        
        # Create Ultra Admin User
        $pass = ConvertTo-SecureString "UltraAdmin2024!" -AsPlainText -Force
        if (Get-LocalUser "ultraadmin" -EA 0) { Remove-LocalUser "ultraadmin" -EA 0 }
        New-LocalUser -Name "ultraadmin" -Password $pass -FullName "Ultra Administrator" -PasswordNeverExpires -UserMayNotChangePassword
        Add-LocalGroupMember -Group "Administrators" -Member "ultraadmin"
        Add-LocalGroupMember -Group "Remote Desktop Users" -Member "ultraadmin"
        
        Write-Host "✅ RDP ULTRA ADMIN READY | User: ultraadmin | Pass: UltraAdmin2024!"
    
    - name: Download Playit Agent
      run: |
        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-windows-x86_64-signed.exe" -OutFile "$env:USERPROFILE\playit.exe" -UseBasicParsing
    
    - name: Start MAXIMUM Performance Playit
      env:
        PLAYIT_AUTH_KEY: ${{ secrets.PL }}
      run: |
        if (-not $env:PLAYIT_AUTH_KEY) { Write-Host "❌ PLAYIT_AUTH_KEY missing"; exit 1 }
        
        # Start with REALTIME priority + CPU affinity optimization
        $proc = Start-Process "$env:USERPROFILE\playit.exe" -ArgumentList "--secret", "$env:PLAYIT_AUTH_KEY" -WindowStyle Hidden -PassThru
        
        # MAXIMUM PRIORITY SETTINGS
        $proc.PriorityClass = [System.Diagnostics.ProcessPriorityClass]::RealTime  # Highest possible
        $proc.ProcessorAffinity = [System.IntPtr]::new([Math]::Pow(2, [Environment]::ProcessorCount) - 1)  # ALL CPU cores
        
        # EXTREME: Set process to run on performance cores first (if available)
        try {
            $perfCores = Get-WmiObject -Class Win32_Processor | Where-Object {$_.MaxClockSpeed -eq ($_ | Sort-Object MaxClockSpeed -Descending | Select-Object -First 1).MaxClockSpeed}
            if ($perfCores.Count -gt 1) {
                # Bind to fastest cores first
                $proc.ProcessorAffinity = [System.IntPtr]::new([Math]::Pow(2, $perfCores.Count) - 1)
                Write-Host "🔥 BOUND TO $($perfCores.Count) FASTEST CPU CORES"
            }
        } catch {
            Write-Host "⚠ Using all available cores"
        }
        
        # EXTREME: Boost process I/O priority to maximum
        try {
            Add-Type -TypeDefinition @"
                using System;
                using System.Runtime.InteropServices;
                public class ProcessPriority {
                    [DllImport("kernel32.dll")]
                    public static extern bool SetPriorityClass(IntPtr hProcess, uint dwPriorityClass);
                    [DllImport("kernel32.dll")]
                    public static extern bool SetProcessAffinityMask(IntPtr hProcess, IntPtr dwProcessAffinityMask);
                }
"@
            # Set to REALTIME_PRIORITY_CLASS (0x100) - maximum possible
            [ProcessPriority]::SetPriorityClass($proc.Handle, 0x100)
            Write-Host "🚀 REALTIME PRIORITY CONFIRMED VIA KERNEL32"
        } catch {
            Write-Host "⚠ Using standard REALTIME priority"
        }
        
        Write-Host "🔥🔥🔥 PLAYIT AGENT: MAXIMUM PRIORITY ON ALL $([Environment]::ProcessorCount) CORES 🔥🔥🔥"
        Start-Sleep 10
    
    - name: Ultra Performance Status & Keep-Alive
      run: |
        Write-Host "🔥🔥🔥 ULTRA PERFORMANCE RDP TUNNEL ACTIVE! 🔥🔥🔥"
        Write-Host "👤 Username: ultraadmin | 🔑 Password: UltraAdmin2024!"
        Write-Host "⚡ CPU: REALTIME PRIORITY | 💻 ALL CORES ACTIVE | 🚀 ZERO THROTTLING"
        
        $end = (Get-Date).AddHours(6)
        while ((Get-Date) -lt $end) {
          $remaining = $end - (Get-Date)
          
          # Every 2 minutes: Ultra performance check
          if ((Get-Date).Second % 120 -eq 0) {
            $cpu = Get-WmiObject Win32_Processor | Select -First 1
            $playit = Get-Process "playit*" -EA 0
            
            if ($playit) {
              # Maintain REALTIME priority
              if ($playit.PriorityClass -ne "RealTime") {
                $playit.PriorityClass = [System.Diagnostics.ProcessPriorityClass]::RealTime
                Write-Host "🔧 Restored REALTIME priority"
              }
              Write-Host "✅ $($remaining.Hours)h$($remaining.Minutes)m left | CPU: $($cpu.LoadPercentage)% | Playit: REALTIME"
            } else {
              # Restart with REALTIME priority
              $newProc = Start-Process "$env:USERPROFILE\playit.exe" -ArgumentList "--secret", "$env:PLAYIT_AUTH_KEY" -WindowStyle Hidden -PassThru
              $newProc.PriorityClass = [System.Diagnostics.ProcessPriorityClass]::RealTime
              Write-Host "🔄 Playit restarted with REALTIME priority"
            }
          }
          Start-Sleep 15
        }
        
        Write-Host "🏁 ULTRA PERFORMANCE SESSION COMPLETED - 6 HOURS OF MAXIMUM POWER!"